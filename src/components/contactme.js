import * as React from "react"
import styled from "styled-components"

const StyledContactMe = styled.div`
  position: absolute;
  color: whitesmoke;
`

const ContactMeComponent = () => {
  return <StyledContactMe>test</StyledContactMe>
}

export default ContactMeComponent
